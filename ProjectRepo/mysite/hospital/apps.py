from django.apps import AppConfig

##########################################
#   CONFIG CLASS
##########################################
# defines the application name as hospital, same as folder and in urls.py reference
class HospitalConfig(AppConfig):
    name = 'hospital'
